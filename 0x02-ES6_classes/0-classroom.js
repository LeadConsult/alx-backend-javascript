// Define a ClassRoom class that takes a maximum student size
class ClassRoom {
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}

// Export the ClassRoom class as the default export
export default ClassRoom;
