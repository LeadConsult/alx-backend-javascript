// Define a constant variable named displayMessage
// The variable holds an arrow function that takes a single parameter 'str'
const displayMessage = (str) => console.log(str);

// Export displayMessage function to make it accessible to other modules
module.exports = displayMessage;
